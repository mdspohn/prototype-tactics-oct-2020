class EffectRenderer extends Renderer {
    constructor(settings) {
        super(settings);
    }

    update(step, effects) {

    }

    render(delta, ctx, camera, location, effect) {
        
    }
}